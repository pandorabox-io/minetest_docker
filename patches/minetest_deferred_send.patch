diff --git a/src/server.cpp b/src/server.cpp
index 90496129e..911cb86a2 100644
--- a/src/server.cpp
+++ b/src/server.cpp
@@ -548,7 +548,11 @@ void Server::AsyncRunStep(bool initial_step)
 
 	{
 		// Send blocks to clients
-		SendBlocks(dtime);
+		send_block_delay++;
+		if (send_block_delay > 5){
+			SendBlocks(dtime);
+			send_block_delay = 0;
+		}
 	}
 
 	if((dtime < 0.001) && !initial_step)
